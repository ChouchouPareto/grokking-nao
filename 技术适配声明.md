# 技术适配声明

> 项目：Grokking恼
> 依据：`Grokking恼_PRD_v1.0_第一版.md` + `AI产品Vibe Coding通用技术栈手册.md` V2.1
> 状态：待产品经理确认（确认后进入第一阶段开发）
> 日期：2026-08-25

---

## 0. 环境自检结果

| 项 | 结果 |
| --- | --- |
| 系统 | macOS (ARM64)，磁盘空间充足 |
| Python | 系统 3.9.6；本机另装 **Python 3.12**（`~/.local/bin/python3.12`），无 3.11 |
| Node | **v24.19.0**（当前 LTS），npm 11.17.0 |
| Git | 2.39.5 可用；**当前目录尚未初始化 git 仓库**（开发前需初始化） |
| 现有代码 | 无，纯新项目（仅 PRD + 三份手册文档） |
| 模型 Key | 暂无（不阻塞开发，验收前补齐） |
| 端口 | 3000、8000 已被占用，需使用替代端口（建议前端 3010、后端 8010） |

---

## 1. 产品形态判断

- **产品类型与核心任务**：3D 空间思考工具（非内容生成器）。用户从"我有一个念头"开始，把关键词放入 3D 空间、建立与发现连接，在 AI 陪伴建议下产生**此前没想到的新联系**。北极星行为 = 用户产生并确认至少一条"这是新发现"的连接。
- **核心交互**：可视化编辑 + 空间画布操作 + 分阶段人工确认（AI 建议须经"接受 / 编辑后接受 / 拒绝"才进入正式网络）。**不是**一次生成，也**不是**以对话为主。
- **开发路径**：**纵向切片**（而非默认的"后端先行"）。
  - 原因：3D 画布、手动建连、AI 候选态的半透明可视化、人工确认等**交互本身决定了产品是否可用**，是 Agent 能力的一部分，不能把 UI 留到最后当"展示壳"。
- **当前阶段范围**：第一阶段只做 **切片 1（核心思考闭环，可先无 AI）**——创建 Idea → 输入/粘贴关键词 → 3D 布局与浏览 → 手动连接 → 标记"这是新发现" → 本地自动保存与恢复。不提前开发 AI 建议、场景系统和管理页。

---

## 2. 采用的默认方案

| 方案 | 采用原因 |
| --- | --- |
| FastAPI + Pydantic + pytest（后端） | AI 建议引擎是典型"模型调用 + 结构化输出校验"，符合默认栈；密钥只留在后端 |
| Next.js + TypeScript + Tailwind（前端） | 桌面 Web 优先，默认前端栈；3D 画布由客户端组件承载 |
| Git 版本管理 | 强制底线；开工时初始化仓库 + `.gitignore` |
| 模型走官方 SDK / 兼容接口 | 待选型（见第 6 节），开发期用 mock 推进 |
| 统一错误结构 | 错误不泄露堆栈，用户侧显示产品语言 |

---

## 3. 触发的按需模块

| 模块 | 触发需求 | 当前阶段 |
| --- | --- | --- |
| 3D 渲染：Three.js + @react-three/fiber + @react-three/drei | PRD 首期核心"3D 关键词渲染页面" | **切片 1 引入** |
| 图布局：d3-force（3D 力导向） | PRD 9.2.1「首次自动布局 + 主动"重新整理"才重排」 | **切片 1 引入** |
| 本地持久化：IndexedDB（idb 或 Dexie） | PRD 12「本地优先、无账号、自动保存」 | **切片 1 引入** |
| AI 结构化输出校验：Pydantic | PRD 10/20「帮我展开 ≤3 条建议，每条件类型/内容/理由/关联节点」 | 切片 2 引入 |
| 语义场景分类（先规则映射，后可选 LLM） | PRD 11「6–8 套氛围，按主要语义方向选择」 | 切片 3 引入 |

**明确不引入**：SQLAlchemy/Alembic（无服务端数据库）、SSE/任务队列（AI 建议为短请求）、OCR/文档解析（无需求）、向量库（无 RAG）。

---

## 4. 偏离或暂缓的默认方案

| 默认方案 | 调整 | 原因 | 影响 | 恢复条件 |
| --- | --- | --- | --- | --- |
| 后端先行 | **纵向切片** | 3D 画布与人工确认是产品核心交互 | 第一阶段即有真实可操作的界面 | —（不回退） |
| FastAPI 承载业务与持久化 | 数据**本地优先**，后端仅作**无状态 AI 代理与建议引擎** | PRD 明确"本地保存、无账号、无云同步"，节点/连接/场景状态存浏览器 IndexedDB | 数据不跨设备，清浏览器数据会丢失（靠 JSON 导出兜底，PRD 已确认） | 未来若做多端同步/账号，再引入服务端存储 |
| Python 3.11.x | **Python 3.12**（本机已装） | 本机无 3.11，3.12 向后兼容 | 无 | — |
| Node 22 LTS | **Node 24**（本机已装，当前 LTS） | 满足"当前活跃 LTS"，无需降级 | 无 | — |
| 关系型数据库 / 向量库 / 任务队列 | 暂缓 | 无触发需求（单用户、本地、无 RAG、无长任务） | 无 | 出现多用户、并发或检索需求时评估 |
| SSE 流式输出 | 暂缓 | "帮我展开"每轮 ≤3 条、短请求 | 无 | 若后续 AI 响应变长或需流式再评估 |

---

## 5. 强制底线检查

- **密钥与隐私**：通过。模型 Key 仅存后端 `.env`（加入 `.gitignore`，只提交 `.env.example`），前端不接触；AI 请求只发送当前 Idea 完成建议所必需文本（PRD 20.1），不读取其他本地 Idea。
- **数据与任务可恢复**：通过。IndexedDB 本地持久化 + 自动保存 + JSON 导出；AI 建议无长任务、无内存态关键数据。
- **输入输出校验**：通过。后端 Pydantic 强校验 AI 结构化输出（格式不合规走有限重试，仍失败走统一错误）；前端对本地/接口数据做运行时校验 + 未知值兜底。
- **错误与日志边界**：通过。统一错误结构，不泄露堆栈、密钥与完整用户文档。
- **测试与真实模型验收**：双层测试。切片 1 以前端单元/组件测试为主（无模型调用）；切片 2 起用 pytest mock 自动化 + 真实 Key 端到端冒烟（Key 未到位时如实标注"待验"，不用 mock 冒充）。

---

## 6. 需要产品经理决定的问题

1. **模型供应商与 Key**：PRD 未指定模型厂商，直接影响成本。建议默认选 **DeepSeek 开放平台**（中文好、JSON 结构输出稳定、成本低），备选火山方舟。请确认：是否有指定/禁用的模型？每月成本上限？开发期可先用 mock 推进，**验收前需提供真实 Key**。
2. **"本地优先"边界的再次确认**：产品数据仅存于浏览器，换设备/清浏览器数据即丢失，只能靠 JSON 导出备份恢复（PRD 12 已确认，此处仅提醒，无需额外决定，若认可请忽略本条）。

其余（3D 渲染库、图布局算法、本地数据库选型、场景素材格式等）均属 PRD 26.3 明确"交由技术设计阶段在既定产品边界内确定"的项，由 AI 自主决策，不逐项请求确认。

---

## 附：切片规划（供参考，非本次确认范围）

| 切片 | 内容 | 依赖 |
| --- | --- | --- |
| 切片 1 | 核心思考闭环：Idea/节点/连接/新发现/本地保存恢复（无 AI） | 前端 3D + IndexedDB |
| 切片 2 | AI 建议引擎：帮我展开 → 3 类建议 → 候选态 → 接受/编辑/拒绝 | FastAPI 代理 + LLM |
| 切片 3 | 语义场景：6–8 套氛围 + 自动选择 + 锁定/手动切换 | 场景素材 |
| 切片 4 | 首页管理 + JSON 导入导出 + 首次引导 | 本地存储 |

第一阶段只交付切片 1。
