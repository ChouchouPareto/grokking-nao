# Grokking恼技术适配声明

> 文档版本：V2.0
>
> 更新日期：2026-08-26
>
> 当前状态：本地 MVP 已验证，尚未进行正式上线改造或云部署
>
> 适配依据：`Grokking恼_PRD_v1.0_第一版.md`、`AI产品Vibe Coding通用技术栈手册.md`、`AI产品Vibe Coding通用前端技术栈手册.md`、`AI Agent 产品上线部署手册.md`

---

## 1. 声明目的

本声明用于说明 Grokking恼 当前实现与通用技术、前端及上线部署规范之间的对应关系，明确：

- 当前已经落地并验证的技术能力；
- 因产品定位而产生的合理偏离；
- 正式上线前必须补齐的能力；
- 需要产品负责人确认后才能改变的隐私、数据和费用边界。

本声明不是部署完成证明，也不授权创建任何云资源。当前仅形成上线准备基线，不执行火山引擎开户、计费资源创建、登录系统建设、云数据库迁移或数据上传。

---

## 2. 当前产品与技术状态

### 2.1 产品形态

Grokking恼是桌面 Web 优先的 3D 空间思考工具。用户把关键词放入空间网络，通过手动连接、空间位置、AI 候选联想和阶段总结，发现并确认此前没有想到的新连接。

AI 是陪伴思考者，不替用户下结论。AI 节点或连接必须经过用户接受、编辑后接受或拒绝，才可进入正式网络。

### 2.2 当前实现

| 层级 | 当前实现 |
| --- | --- |
| 前端 | Next.js 16.3.2、React 19、TypeScript、Tailwind CSS 4 |
| 3D 渲染 | Three.js、React Three Fiber、Drei、d3-force-3d |
| 客户端状态 | Zustand |
| 本地数据 | IndexedDB（`idb`），数据库名 `grokking` |
| 后端 | FastAPI、Pydantic、httpx，Python 3.12 |
| AI 模型 | DeepSeek OpenAI 兼容接口，可通过环境变量替换；无 Key 时进入 mock 模式 |
| 测试 | 后端 pytest；前端 lint、typecheck、build、真实 Chrome E2E |
| 当前端口 | 前端 3010，后端 8010 |

### 2.3 当前验证基线

- 后端测试：23 项通过。
- 前端核心流程 E2E：24/24 通过。
- 前端 AI 流程与内容安全 E2E：11/11 通过。
- TypeScript、ESLint、Next.js 生产构建通过。
- 敏感内容实际请求返回 `422 content_blocked`，不产生 AI 候选内容。

以上结果证明当前本地 MVP 可运行，不等同于正式生产环境验收。

---

## 3. 对通用技术方案的采用情况

| 通用要求 | 适配结论 | 当前实现 |
| --- | --- | --- |
| 前后端分离 | 已采用 | Next.js 前端 + FastAPI AI 代理后端 |
| 类型与结构校验 | 已采用 | TypeScript strict + Pydantic 响应模型 |
| 密钥只在后端 | 已采用 | `LLM_API_KEY` 只由后端环境变量读取，前端不持有模型 Key |
| 统一错误响应 | 部分采用 | AI 失败、解析失败、内容拦截已使用结构化错误；尚无全局 trace_id |
| 模型输出不可直接写入正式数据 | 已采用 | 候选态必须由用户接受或编辑后接受 |
| 短请求不强制流式 | 已采用 | 每轮最多 3 条建议，当前使用普通 HTTP 请求 |
| 无真实需求不引入数据库、队列、向量库 | 已采用 | 当前无服务端业务数据库、任务队列和 RAG |
| 真实浏览器闭环验收 | 已采用 | Playwright-core 复用本机 Chrome 验证 3D 操作和 AI 闭环 |

---

## 4. 因产品定位产生的合理偏离

### 4.1 本地优先，而不是服务端业务数据库

PRD 明确 MVP 无账号、无云同步，Idea、节点、连接、候选处理状态和阶段总结保存在浏览器 IndexedDB。后端目前是无状态 AI 代理，不保存用户图谱。

因此，上线手册中的 `DATABASE_URL`、SQLite `/tmp`、TOS 数据库备份和 RDS PostgreSQL 当前均未触发。不能为了照搬手册而把全部本地 Idea 自动上传云端；这会扩大隐私范围，属于需要产品负责人重新确认的红线。

### 4.2 暂无对象存储需求

当前产品没有用户上传的图片、音频、视频或文档文件。背景、图形和 UI 均由前端代码与静态资源提供，因此当前不需要 TOS。

当后续加入图片导出、附件、生成式 3D 世界或用户素材上传时，再引入对象存储与文件元数据管理。

### 4.3 前端是产品本体

本项目不能按“先后端、后界面”的常规路径拆分。3D 画布、节点空间位置、镜头控制和候选态交互直接决定产品是否可用，因此采用纵向切片开发。

### 4.4 桌面 Web 为主要目标

正式验收优先覆盖桌面 Chrome、Edge、Safari。移动端当前只要求能够打开和浏览，复杂 3D 编辑不作为 MVP 的 P0 门槛。

---

## 5. 与上线部署手册的差距

### 5.1 上线前 P0 阻断项

| 阻断项 | 当前状态 | 上线前要求 |
| --- | --- | --- |
| 登录与数据隔离 | 未实现 | 若作为正式公网多用户产品，必须实现邀请码或其他登录方式，并确保用户数据隔离 |
| 数据可恢复 | 不满足 | IndexedDB 会因清理浏览器数据而丢失；至少先完成 JSON 导入/导出，正式云同步需另行确认 |
| 前端生产代理 | 未实现 | `next.config.ts` 尚无 `standalone` 输出和 `/api/*` 同源 rewrites |
| 生产 CORS | 未实现 | 后端当前只允许 `localhost:3010` 与 `127.0.0.1:3010` |
| 访问限流与成本保护 | 未实现 | AI 接口公网开放前必须增加身份校验、速率限制、请求体大小限制和额度保护 |
| 线上日志与追踪 | 未实现 | 需要结构化日志、request/trace_id、模型耗时和错误分类；不得记录密钥和完整敏感原文 |
| 打包排除 | 部分满足 | `.env`、`.venv` 已被 Git 忽略，但尚无 `.vefaasignore`，必须在部署前补齐并验证 |
| 云端环境变量 | 未配置 | 目前只有本地 `.env`；正式环境必须使用平台 Secret/环境变量 |
| 正式 HTTPS 地址 | 未部署 | 当前只有 localhost，没有公网访问地址 |

### 5.2 P1 生产增强项

- Sentry 或等价错误监控与告警；
- 运行时健康监控、AI 请求成功率和耗时统计；
- WebGL 不可用时的简化浏览或数据导出路径；
- 内容安全规则版本化、审计统计与人工复核机制；
- 跨浏览器、移动端浏览和弱性能设备验证；
- 依赖与供应链漏洞扫描。

---

## 6. 建议的上线分级

### 6.1 A 级：本地演示

当前已经达到。适合产品验证和面对面演示，不对公网开放。

### 6.2 B 级：受控内测

可采用火山引擎 veFaaS 的前后端双应用方案，但必须至少补齐：

1. HTTPS 正式入口；
2. 网关或应用级访问控制；
3. AI 接口限流与额度上限；
4. JSON 导入/导出；
5. 生产环境变量与密钥轮换；
6. 结构化日志与基础告警。

受控内测可继续使用 IndexedDB，但必须明确告知：数据只保存在当前浏览器，不跨设备同步，清缓存可能丢失。

### 6.3 C 级：正式公网产品

除 B 级能力外，还必须实现登录、用户隔离、服务端持久化和可恢复备份。是否把全部 Idea 上传云端会改变 PRD 的本地优先与隐私边界，必须先由产品负责人确认，再开始开发。

---

## 7. 火山引擎 veFaaS 适配结论

### 7.1 后端

FastAPI 适合部署为独立 veFaaS 应用。建议启动命令：

```bash
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

当前后端无数据库和文件写入，因此暂不需要 `DATABASE_URL` 或 `/tmp` 数据目录。必须配置的生产环境变量为：

- `LLM_API_KEY`
- `LLM_BASE_URL`
- `LLM_MODEL`
- `LLM_TIMEOUT`

正式公网前还需增加认证、限流、trace_id 和生产允许来源配置。

### 7.2 前端

Next.js 应作为第二个 veFaaS 应用独立部署。当前尚未完成以下生产适配：

- `output: "standalone"`；
- 通过 `BACKEND_URL` 配置同源 `/api/*` rewrites；
- standalone 静态资源复制；
- `node server.js` 生产启动；
- 对公网域名、HTTPS 和 WebGL 的真实验证。

当前前端使用 `NEXT_PUBLIC_API_BASE_URL` 直接访问后端。该变量会暴露在浏览器且构建时固化，只能保存公开地址，不能放任何密钥。

### 7.3 云服务触发判断

| 云服务 | 当前是否需要 | 说明 |
| --- | --- | --- |
| veFaaS | 上线时需要 | 分别运行前端和后端 |
| Serverless API 网关 | 上线时需要 | 提供 HTTPS 访问入口 |
| TOS | 当前不需要 | 无用户文件；正式云数据备份或素材上传时再启用 |
| RDS PostgreSQL | 当前不需要 | 当前本地优先；正式多用户云同步时再评估 |
| TLS 日志服务 | 内测可选、正式建议 | 初期可用平台日志，正式阶段接结构化日志和告警 |
| VPC | 当前不需要 | 使用 RDS 等私网资源时再启用 |

创建任何计费资源前，必须向产品负责人说明资源、计费方式和费用量级并获得确认。

---

## 8. 安全与合规适配

### 8.1 已实现

- 模型 Key 仅由后端环境变量读取；
- AI 请求只发送当前 Idea 完成建议所需的标题、节点、连接和拒绝摘要；
- AI 建议须经用户确认才进入正式网络；
- 输入调用模型前执行内容安全检查；
- 模型可返回结构化安全拒绝；
- 模型输出再次检查，命中后不生成候选；
- 前端显示“内容安全提醒”，不把拦截当作普通网络错误；
- 错误响应不返回 Python 堆栈。

### 8.2 上线前必须处理

- 曾在协作对话中出现过模型密钥。该密钥不得继续用于生产，上线前必须在供应商后台撤销并生成新 Key；文档和日志中不得回显旧值。
- 当前内容安全是规则、模型拒绝和输出检查的组合，不应宣称覆盖率为 100%。正式上线应补充规则更新机制、误伤处理和必要的人工审核通道。
- AI 接口缺少身份认证、限流和滥用防护，不能直接裸露到公网。
- 生产日志不得记录完整用户 Idea、敏感原文、邀请码、token 或模型 Key。

---

## 9. 工程与依赖适配

- 当前 Node.js 版本为 24.19.0，npm 为 11.17.0，Python 为 3.12.13。
- 前端同时存在 `package-lock.json`、`pnpm-lock.yaml` 和 `pnpm-workspace.yaml`。正式构建前应选定单一包管理器；当前验证命令以 npm 为准，建议以 `package-lock.json` 作为发布锁文件。
- `frontend/.npmrc` 含本机绝对缓存路径，不应作为云端构建依赖；上线前需移除该机器绑定或改成可移植配置。
- 当前仓库存在尚未提交的业务和文档改动。部署前必须先整理版本、完成测试并形成明确的发布提交。

---

## 10. 关键红线与决策点

以下事项不在本轮文档工作中执行，后续开始前必须获得产品负责人确认：

1. 是否从“本地优先、无账号”升级为“账号 + 云同步”；
2. 是否把用户全部 Idea、节点和连接上传云端；
3. 内测还是正式公网发布；
4. 火山引擎账号类型、预算上限和计费资源；
5. 登录方式采用邀请码、第三方登录还是企业 SSO；
6. 数据保留、删除、备份和恢复周期；
7. 新模型 Key 的创建与使用授权。

除上述隐私、不可逆数据、计费和产品边界事项外，框架配置、构建脚本、日志格式、CORS、限流实现等属于后续技术执行范围，可在确认上线级别后按本声明推进。

---

## 11. 结论

Grokking恼当前适合继续作为本地 MVP 或产品演示版本使用；3D 核心交互、AI 联想、阶段总结、本地保存和内容安全闭环已通过验证。

当前不满足《AI Agent 产品上线部署手册》对正式公网产品的全部底线，主要缺口是登录隔离、数据可恢复、生产代理、限流与监控。因此本轮只完成文档基线，不进入部署。待产品负责人确定发布级别与数据边界后，再按“受控内测”或“正式公网产品”路径实施。
