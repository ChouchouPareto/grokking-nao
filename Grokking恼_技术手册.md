# Grokking恼技术手册

> 文档版本：V1.0
>
> 更新日期：2026-08-26
>
> 适用范围：当前本地 MVP、后续受控内测与正式上线准备
>
> 当前状态：本地运行与测试通过，尚未部署到公网

---

## 1. 文档用途

本手册记录 Grokking恼 当前真实技术架构、开发运行方式、数据与 AI 契约、安全边界、测试方法和上线准备要求，供后续开发、交接、部署与排错使用。

本手册以当前代码为准。标注为“计划”或“上线前”的内容尚未实现，不应视为现有能力。

---

## 2. 产品技术摘要

Grokking恼是以 3D 关键词网络为核心的空间思考工具。用户可以：

1. 创建一个 Idea；
2. 添加、编辑、删除和拖动关键词节点；
3. 手动建立连接并标记“新发现”；
4. 使用 WASD/方向键在空间游走，Q/E 左右转向；
5. 让 AI 识别意图、发散节点、建议连接或提出追问；
6. 接受、编辑或拒绝 AI 候选；
7. 生成并保存阶段总结；
8. 在当前浏览器中自动保存并恢复项目。

核心原则：AI 只提供候选，未经用户确认不得写入正式网络。

---

## 3. 系统架构

```text
浏览器
├── Next.js 页面与交互
├── React Three Fiber 3D 画布
├── Zustand 客户端状态
└── IndexedDB 本地 Idea 数据
        │
        │ 仅发送当前 Idea 的必要文本与图结构
        ▼
FastAPI AI 代理
├── Pydantic 请求/响应校验
├── 输入内容安全检查
├── AI Prompt 与结构化解析
├── 输出内容安全检查
└── DeepSeek OpenAI 兼容接口
```

### 3.1 架构边界

- 前端是产品本体，负责 3D 渲染、交互和本地业务数据。
- 后端是无状态 AI 代理，不保存 Idea、节点或连接。
- AI 服务不可用时，手动创建、连接、编辑和本地保存仍可使用。
- 当前没有服务端账号、业务数据库、对象存储、任务队列或向量库。

---

## 4. 技术栈与版本

### 4.1 前端

| 技术 | 版本/用途 |
| --- | --- |
| Next.js | 16.3.2，App Router |
| React | 19.2.8 |
| TypeScript | 5.x，严格类型检查 |
| Tailwind CSS | 4.x |
| Three.js | 0.185.x，3D 场景 |
| @react-three/fiber | 9.x，React 3D 渲染层 |
| @react-three/drei | 10.x，OrbitControls、Html、Line 等辅助能力 |
| d3-force-3d | 3.x，3D 力导向布局 |
| Zustand | 5.x，全局客户端状态 |
| idb | 8.x，IndexedDB 封装 |
| Playwright Core | 1.62.x，真实 Chrome E2E |

### 4.2 后端

| 技术 | 用途 |
| --- | --- |
| Python 3.12 | 运行时 |
| FastAPI | HTTP API |
| Pydantic | 输入输出模型与校验 |
| httpx | 调用 OpenAI 兼容模型接口 |
| python-dotenv | 本地读取 `.env` |
| pytest | 后端自动化测试 |

### 4.3 当前开发环境

- Node.js：24.19.0
- npm：11.17.0
- Python：3.12.13
- 前端端口：3010
- 后端端口：8010

前端发布基线当前使用 npm 与 `package-lock.json`。仓库中的 pnpm 锁文件不应与 npm 混合作为生产安装依据。

---

## 5. 目录结构

```text
Grokking恼/
├── frontend/
│   ├── src/app/                  # 首页、布局、Idea 路由和全局样式
│   ├── src/components/idea/      # 3D 画布、工作区、侧栏、AI 与总结面板
│   ├── src/components/ui.tsx     # 基础 UI 组件
│   ├── src/lib/                  # 状态、数据库、AI 客户端、图布局、类型与工具
│   └── scripts/                  # 核心与 AI 端到端测试
├── backend/
│   ├── app/main.py               # FastAPI 入口与错误映射
│   ├── app/schemas.py            # API 数据模型
│   ├── app/config.py             # 模型环境变量
│   ├── app/services/llm.py       # 模型调用
│   ├── app/services/prompts.py   # Prompt 契约
│   ├── app/services/suggestions.py
│   ├── app/services/content_safety.py
│   └── tests/                    # 后端测试
├── docs/                         # 阶段开发文档与前端适配声明
├── 技术适配声明.md
└── Grokking恼_技术手册.md
```

---

## 6. 本地安装与启动

### 6.1 后端

首次安装：

```bash
cd backend
~/.local/bin/python3.12 -m venv .venv
.venv/bin/pip install -r requirements.txt
cp .env.example .env
```

启动：

```bash
cd backend
.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8010
```

健康检查：

```bash
curl http://127.0.0.1:8010/api/v1/health
```

返回中的 `mock: true` 表示没有配置模型 Key，系统使用确定性 mock 建议。

### 6.2 前端

```bash
cd frontend
npm install
npm run dev -- -p 3010
```

浏览器打开：

```text
http://localhost:3010
```

### 6.3 正常关闭

在对应终端按 `Ctrl+C` 停止前端或后端。不要通过强制删除项目目录来停止服务。

---

## 7. 环境变量与密钥

### 7.1 后端变量

| 变量 | 是否必填 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `LLM_API_KEY` | 真实模型必填 | 空 | 空值进入 mock 模式 |
| `LLM_BASE_URL` | 否 | `https://api.deepseek.com` | OpenAI 兼容接口根地址 |
| `LLM_MODEL` | 否 | `deepseek-chat` | 模型名称 |
| `LLM_TIMEOUT` | 否 | `30` | 请求超时秒数 |

本地变量放在 `backend/.env`。该文件已被 Git 忽略，不得提交。

### 7.2 前端变量

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `NEXT_PUBLIC_API_BASE_URL` | `http://localhost:8010` | 当前浏览器直连后端的公开地址 |

`NEXT_PUBLIC_*` 会进入浏览器构建产物，只能保存公开配置，绝不能放模型 Key、AccessKey、密码或 token 密钥。

### 7.3 密钥处理规则

- 不进入 Git、前端代码、截图、日志或文档；
- 线上只放在部署平台的 Secret/环境变量中；
- 曾在协作对话中出现过的模型 Key 上线前必须撤销并轮换；
- 排错输出只确认“是否配置”，不得打印密钥值；
- 火山引擎 AK/SK 仅在真正开始部署且获得授权后使用。

---

## 8. 前端核心设计

### 8.1 页面

| 路由 | 作用 |
| --- | --- |
| `/` | 项目列表、新建项目、复制与删除项目 |
| `/idea/[id]` | 3D 思考空间 |

### 8.2 3D 交互

- 单击关键词：选中并聚焦到画布几何中心；
- 拖动关键词胶囊：调整节点位置并固定；
- 双击空白画布：在对应空间位置添加节点；
- Delete：删除选中节点；
- C：进入或退出连接模式；
- WASD/方向键：前后左右游走；
- Q/E：向左/向右转向；
- Shift：移动与转向加速；
- 鼠标拖动画布：OrbitControls 旋转；
- 滚轮：缩放。

### 8.3 侧栏

- 左右侧栏默认收起；
- 鼠标触碰左右边缘时临时滑入；
- 离开后延迟收起，避免误触抖动；
- 点击边缘箭头可固定或取消固定；
- 内容安全拦截时右侧提醒区主动显示。

### 8.4 图布局

- 节点使用 d3-force-3d 自动整理；
- 用户拖动后的节点可标记为固定，普通更新不得强制重排；
- “重新整理”才释放固定并重新布局；
- 图数据与 Three.js 渲染对象分离。

---

## 9. 数据与持久化

### 9.1 当前数据位置

所有 Idea 数据保存在当前浏览器 IndexedDB：

- 数据库：`grokking`
- Object Store：`ideas`
- IndexedDB 数据库版本：1
- 业务 Schema 版本：由 `SCHEMA_VERSION` 管理

### 9.2 保存内容

- Idea 标题、原始念头和时间；
- 节点、位置、来源、正式/候选状态；
- 连接、说明、新发现标记；
- 已拒绝建议摘要；
- 意图画像和自动关系扫描设置；
- 阶段总结历史。

### 9.3 当前限制

- 数据不跨浏览器、不跨设备；
- 清除站点数据或浏览器数据会丢失 Idea；
- 当前尚未实现 PRD 要求的 JSON 导入/导出；
- 后端无法恢复浏览器本地数据；
- 当前没有服务端备份。

在 JSON 导入/导出完成之前，不应把当前版本描述为“数据可恢复的正式线上产品”。

---

## 10. AI 建议引擎

### 10.1 请求模式

| 模式 | 用途 | 输出限制 |
| --- | --- | --- |
| `intent_profile` | 只更新意图画像 | 不返回建议 |
| `deep_expand` | 全局发散 | 最多 3 条节点、连接或追问，覆盖多个维度 |
| `relation_probe` | 新节点关系扫描 | 只返回连接，至少一端为触发节点 |
| `node_brainstorm` | 围绕选中节点发散 | 返回 3 个短语型节点候选 |

### 10.2 意图画像

后端根据标题、原始念头、节点与连接识别：

- 主要意图；
- 当前思考阶段；
- 已出现的思考维度；
- 仍缺失的维度；
- 置信度。

意图画像用于选择联想方向，避免所有 Idea 都落入“项目成本、目标客户”等固定模板。

### 10.3 候选状态

AI 返回的内容先进入候选态：

- 节点候选：半透明出现在关联节点周围；
- 连接候选：以弱化线条显示；
- 追问：在右侧建议面板显示；
- 接受后才写入正式网络；
- 编辑后接受使用用户修改内容；
- 拒绝后写入拒绝摘要，下一轮避免原样重复。

### 10.4 失败策略

- 网络失败或模型失败：显示错误与重试，手动画布不被阻塞；
- 模型 JSON 首次解析失败：进行一次结构修复请求；
- 连续无法解析：不再用本地模板扩写，返回空建议；
- 内容安全命中：进入 `blocked` 状态，不提供直接重试。

---

## 11. 内容安全

### 11.1 拦截范围

AI 联想对政治敏感、违法犯罪、暴力、自残、色情、仇恨和明显违背公序良俗的内容执行拒绝与提醒。

### 11.2 三层防护

1. **输入前置检查**：检查标题、原始念头、节点、连接说明和拒绝摘要；命中后不调用模型。
2. **模型结构化拒绝**：Prompt 要求模型返回 `safety_refusal`，不得继续联想或通过改写规避。
3. **输出检查**：再次检查候选内容、理由、维度和关系说明；命中后不写入画布。

### 11.3 API 行为

命中时返回：

```json
{
  "detail": {
    "error": {
      "code": "content_blocked",
      "message": "该内容涉及政治敏感、违法犯罪、暴力、色情或违背公序良俗的信息，AI 无法继续联想。请修改相关关键词后再试。",
      "category": "分类"
    }
  }
}
```

HTTP 状态码为 `422`。前端显示“内容安全提醒”，清空候选且不显示普通“重试”按钮。

### 11.4 边界说明

当前规则不能保证 100% 识别所有表达，也可能误伤历史研究、新闻分析或教育场景。正式上线需要版本化规则、误伤反馈与必要的人工复核，不应宣传为绝对审核能力。

---

## 12. API 契约

### 12.1 健康检查

```http
GET /api/v1/health
```

示例：

```json
{"status":"ok","mock":false}
```

### 12.2 AI 联想

```http
POST /api/v1/ai/suggestions
Content-Type: application/json
```

核心请求字段：

- `idea_id`
- `seed_text`
- `title`
- `nodes[]`
- `edges[]`
- `rejected_summary[]`
- `focused_node_id`
- `mode`
- `trigger_node_ids[]`

成功响应包含 `request_id`、`intent_profile` 和最多 3 条 `suggestions`。

主要错误：

| HTTP | code | 说明 |
| --- | --- | --- |
| 422 | `content_blocked` | 内容安全拒绝 |
| 502 | `ai_failed` | 模型暂时不可用 |
| 502 | `ai_unparsable` | 模型结果无法解析 |

### 12.3 阶段总结

```http
POST /api/v1/ai/summary
Content-Type: application/json
```

输入正式节点、正式连接、总结范围和可选聚焦节点，返回标题、概览、主题、关键连接、未决问题和下一步方向。

当前阶段总结接口尚未接入与联想接口相同的内容安全前置分类，正式上线前应统一安全边界。

---

## 13. 测试与交付门槛

### 13.1 后端

```bash
cd backend
.venv/bin/python -m pytest tests/ -q
```

当前基线：23 项通过。覆盖解析、意图识别、建议清洗、关系约束、拒绝去重、阶段总结以及内容安全输入/输出拦截。

### 13.2 前端静态检查

```bash
cd frontend
npm run typecheck
npm run lint
npm run build
```

### 13.3 前端端到端

```bash
cd frontend
npm run e2e
npm run e2e:ai
```

当前基线：

- 核心流程：24/24；
- AI 与安全流程：11/11。

E2E 需要本机安装 Google Chrome；AI E2E 需要后端已启动。

### 13.4 发布门槛

每次准备发布至少要求：

1. 后端测试通过；
2. TypeScript 和 ESLint 通过；
3. Next.js 生产构建通过；
4. 核心 E2E 与 AI E2E 通过；
5. 无模型 Key、云 AK/SK 或密码进入 Git；
6. 发布版本有明确提交记录和回滚点。

---

## 14. 上线准备方案

### 14.1 当前不执行部署

本手册只记录方案。当前没有创建火山引擎账号、API 网关、函数、TOS、RDS 或任何计费资源。

### 14.2 推荐部署形态

采用两个独立 veFaaS 应用：

```text
公网 HTTPS
  └── Serverless API 网关
      ├── Next.js 前端应用（正式访问入口）
      └── FastAPI 后端应用（AI 代理）
```

前端通过同源 `/api/*` rewrites 访问后端，避免在浏览器中维护生产 CORS 地址。

### 14.3 后端计划启动命令

```bash
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 14.4 前端计划构建与启动

上线前需要先实施：

- `next.config.ts` 设置 `output: "standalone"`；
- 使用 `BACKEND_URL` 配置 rewrites；
- 构建后复制 `.next/static`；
- 以 `node server.js`、端口 3000 启动。

以上均是计划，当前代码尚未完成这些生产适配。

### 14.5 上线级别

| 级别 | 数据策略 | 必要能力 |
| --- | --- | --- |
| 本地演示 | IndexedDB | 当前已满足 |
| 受控内测 | IndexedDB + JSON 备份 | HTTPS、访问控制、限流、Secret、日志 |
| 正式公网 | 服务端持久化 + 可恢复备份 | 登录、用户隔离、云数据、删除与恢复机制、监控告警 |

正式公网方案会扩大数据上传范围，必须先确认隐私与产品边界。

---

## 15. 生产环境待办

### 15.1 代码与配置

- [ ] 完成 JSON 导入/导出；
- [ ] 确认发布级别；
- [ ] 完成 Next.js standalone 与同源 rewrites；
- [ ] 配置生产 CORS 或完全改为同源代理；
- [ ] 增加 AI 身份校验、限流、请求大小和额度保护；
- [ ] 增加结构化日志与 trace_id；
- [ ] 统一阶段总结与联想的安全边界；
- [ ] 创建并验证 `.vefaasignore`；
- [ ] 统一 npm 锁文件，处理本机绝对 `.npmrc`；
- [ ] 轮换模型 Key；
- [ ] 完成跨浏览器与 WebGL 降级验证。

### 15.2 产品负责人确认

- [ ] 受控内测还是正式公网；
- [ ] 是否引入账号与云同步；
- [ ] 是否允许上传全部 Idea；
- [ ] 登录方式；
- [ ] 数据保留、删除、备份周期；
- [ ] 火山引擎账号与预算；
- [ ] 模型费用上限。

### 15.3 云平台

- [ ] 注册并实名认证；
- [ ] 创建最小权限子账号；
- [ ] 开通实际需要的服务权限；
- [ ] 安装并阅读 veFaaS 官方 Skill；
- [ ] 检查现有 Serverless 网关；
- [ ] 创建资源前确认费用；
- [ ] 先配置环境变量，再首次发布；
- [ ] 首次发布后再设置预留实例；
- [ ] 取得并验证 HTTPS 地址。

---

## 16. 常见问题排查

| 现象 | 检查与处理 |
| --- | --- |
| 前端提示无法连接 AI | 检查 8010 后端是否启动、`NEXT_PUBLIC_API_BASE_URL` 是否正确、CORS 是否允许当前来源 |
| AI 一直返回 mock | 检查后端 `LLM_API_KEY` 是否为空；不要打印 Key 本身 |
| AI 返回 502 | 检查模型地址、模型名、超时和供应商状态；用户侧不显示堆栈 |
| AI 返回 422 | 内容安全拦截，修改相关关键词后重新发起，不应直接重复请求 |
| 页面刷新后项目不见 | 检查是否换浏览器、无痕窗口或清理站点数据；当前无服务端恢复 |
| 3D 画布黑屏或空白 | 检查浏览器 WebGL、GPU 加速与控制台错误；再尝试全局视图 |
| 节点被侧栏遮挡 | 关闭侧栏或使用全局视图；画布会根据侧栏状态调整可视区域 |
| 部署后 API 跨域失败 | 优先改成前端同源 rewrites；不要简单放开 `*` 并同时传凭证 |
| veFaaS 启动找不到 uvicorn | 使用 `python -m uvicorn`，不要依赖 PATH 中的 `uvicorn` 命令 |
| 打包体积异常大 | 检查 `.vefaasignore` 是否排除 `.venv`、`.env`、缓存和构建残留 |

---

## 17. 运维与隐私规则

- 日志只记录 request_id、耗时、模式、建议数量、错误分类等元信息；
- 不记录完整 Idea 原文、模型 Key、token、邀请码或云密钥；
- 内容安全日志只记录类别和统计，不回显敏感全文；
- AI 供应商切换通过后端环境变量完成，前端契约保持稳定；
- 数据结构升级必须通过 `SCHEMA_VERSION` 和兼容迁移，不能覆盖无法识别的旧数据；
- 删除、导入和迁移属于高风险操作，必须先验证目标和恢复路径；
- 创建云资源、扩大模型额度或启用包月实例前必须让产品负责人知情。

---

## 18. 版本维护

当以下内容变化时必须更新本手册：

- 前后端框架、Node/Python 或关键依赖版本；
- API 字段、AI 模式、错误码或内容安全规则；
- IndexedDB Schema；
- 登录、云同步、数据库或对象存储方案；
- 部署平台、域名、环境变量或启动命令；
- 测试基线和正式访问地址。

更新文档时必须区分“当前已实现”和“后续计划”，避免把设计方案写成已上线事实。
